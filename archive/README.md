# Historical Archive

This directory contains historical reference snapshots, not active application
code for the GWAS engine.

Default repository searches should ignore this tree. Use an explicit opt-in
search such as `rg -u <pattern> archive/` when comparing against the archived
reference implementation.

Do not delete or move archive snapshots from `main` without first preserving
the intended snapshot on a dedicated branch, tag, or external artifact.
