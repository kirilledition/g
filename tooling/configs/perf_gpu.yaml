defaults:
  - benchmark_regenie2_binary_hot
  - _self_

tool:
  output_dir: results/perf/gpu/regenie2_binary_hot
  summary_path: results/perf/gpu/regenie2_binary_hot_summary.json

hydra:
  output_subdir: null
  job:
    chdir: false
  run:
    dir: /tmp/g-hydra-runs
