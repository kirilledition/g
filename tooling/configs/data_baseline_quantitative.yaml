defaults:
  - dataset: local_1kg
  - _self_

tool:
  name: regenie_baseline
  data_directory: ${dataset.data_directory}
  trait: quantitative
  verify_only: false

hydra:
  output_subdir: null
  job:
    chdir: false
  run:
    dir: /tmp/g-hydra-runs
