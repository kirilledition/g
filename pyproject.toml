[project]
name = "g"
version = "0.1.0"
readme = "README.md"
authors = [
    { name = "kirill denisov", email = "101817863+kirilledition@users.noreply.github.com" },
]
requires-python = ">=3.14,<3.15"
dependencies = [
    "click>=8.3.1",
    "jax[cpu]>=0.10.1",
    "numpy>=2.4.5",
]

[dependency-groups]
dev = [
    "gputil>=1.4.0",
    "maturin>=1.13.3,<2.0",
    "polars>=1.40.1",
    "psutil>=7.2.2",
    "py-cpuinfo>=9.0.0",
    "pyarrow>=24.0.0",
    "pytest>=9.0.3",
    "pytest-cov>=7.1.0",
    "ruff>=0.15.13",
    "ty>=0.0.36",
]
gpu = ["jax[cuda12]>=0.10.0"]

[project.scripts]
g = "g:main"
g-regenie = "g.cli:regenie_main"

[build-system]
requires = ["maturin>=1.13.3,<2.0"]
build-backend = "maturin"

[tool.maturin]
features = ["extension-module"]
module-name = "g._core"
python-packages = ["g"]
python-source = "src"

[tool.uv]
cache-keys = [
    { file = "pyproject.toml" },
    { file = "src/**/*.rs" },
    { file = "Cargo.toml" },
    { file = "Cargo.lock" },
]

[tool.ruff]
line-length = 120
target-version = "py314"
exclude = ["archive/", "scripts/", ".git/"]

[tool.ruff.lint]
fixable = ["ALL"]
select = [
    "F",   # Pyflakes rules
    "W",   # PyCodeStyle warnings
    "E",   # PyCodeStyle errors
    "I",   # Sort imports properly
    "UP",  # Warn if certain things can changed due to newer Python versions
    "C4",  # Catch incorrect use of comprehensions, dict, list, etc
    "FA",  # Enforce from __future__ import annotations
    "ISC", # Good use of string concatenation
    "ICN", # Use common import conventions
    "RET", # Good return practices
    "SIM", # Common simplification rules
    "TID", # Some good import practices
    "TC",  # Enforce importing certain types in a TYPE_CHECKING block
    "PTH", # Use pathlib instead of os.path
    "TD",  # Be diligent with TODO comments
    "NPY", # Some numpy-specific things
    "PD",  # Pandas-specific rules
    "N",   # Naming conventions
    "RUF", # Ruff-specific rules
    "A",   # detect shadowed builtins
    "BLE", # disallow catch-all exceptions
    "COM", # enforce trailing comma rules
    "DTZ", # require strict timezone manipulation with datetime
    "FBT", # detect boolean traps
    "D",   # docstrings
]
ignore = [
    "COM812", # enforce trailing comma rules
    "D200",   # one-line docstring should fit on one line
    "D203",   # allow blank line before class docstrings
    "D213",   # allow free-form second summary line
]

[tool.ruff.lint.per-file-ignores]
"tests/*" = [
    "D", # docstrings
]
"scripts/*" = [
    "D",    # docstrings
    "T201", # allow print() in scripts
]

[tool.ruff.lint.flake8-import-conventions]
banned-from = ["typing"]

[tool.ruff.lint.flake8-import-conventions.extend-aliases]
"jax.numpy" = "jnp"
"numpy.typing" = "npt"
"polars" = "pl"

[tool.ruff.lint.flake8-tidy-imports]
ban-relative-imports = "all"

[tool.pytest.ini_options]
markers = [
    "phase0_data: requires local Phase 0 data or generated baseline artifacts.",
    "phase1_parity: exercises Phase 1 parity and benchmark evaluation flows against external baselines.",
]

[tool.coverage.run]
branch = true
source = ["src/g"]

[tool.coverage.report]
fail_under = 90
omit = ["src/g/__init__.py"]
show_missing = true
skip_covered = false
